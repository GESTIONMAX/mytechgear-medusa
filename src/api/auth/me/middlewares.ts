/**
 * Middlewares for /me route
 *
 * This route should NOT have admin API token middleware
 * It handles its own session-based authentication
 */

// Export empty array to override any parent middleware
export const middlewares = [];
