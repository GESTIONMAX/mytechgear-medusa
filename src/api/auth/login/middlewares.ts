/**
 * Middlewares for login route
 *
 * Login route should NOT have authentication middleware
 * (users can't be authenticated before logging in!)
 */

// Export empty array to override any parent middleware
export const middlewares = [];
