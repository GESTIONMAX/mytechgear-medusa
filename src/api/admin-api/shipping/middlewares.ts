/**
 * Middleware configuration for /admin-api/shipping routes
 *
 * Export empty array to prevent Medusa from applying default admin middleware
 * Note: Routes under /admin-api are NOT subject to Medusa's automatic admin middleware
 */
export const middlewares = [];
