/**
 * Middleware configuration for /admin/pricing routes
 *
 * Export empty array to prevent Medusa from applying default admin middleware
 * Authentication is handled by the route-level middleware (authenticateAdmin)
 */
export const middlewares = [];
